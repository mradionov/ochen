import type { VideoTimelineClip } from '../../../features/video_timeline/video_timeline_selectors';

export const VideoClipCropTab = (_props: {
  timelineClip: VideoTimelineClip;
}) => {
  return <div>Video Clip Crop Tab Content</div>;
};
